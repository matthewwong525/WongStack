# wiki-root Specification

## Purpose
Where WongStack keeps its own progressive-disclosure wiki (`wiki/`), the generic `wiki/`-then-`docs/` resolution every skill uses in a target repo, and the rule that no live payload file hardcodes `docs/` as the wiki root.

## Requirements
