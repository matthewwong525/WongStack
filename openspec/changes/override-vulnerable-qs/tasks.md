## 1. App scaffold

- [x] 1.1 Add `"overrides": { "qs": "^6.16.0" }` to `app/package.json` and refresh `app/package-lock.json`, per review.html#/override/added

## 2. Release

- [x] 2.1 Bump `VERSION` to 20.1.1 and add the `CHANGELOG.md` entry
- [ ] 2.2 Pass CI through `/save`
