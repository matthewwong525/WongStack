# Update exploration

The former adaptation pipeline has been replaced by the bounded [`/explore`](../../explore/SKILL.md) that [`/plan`](../../plan/SKILL.md) runs when [`/wong-sync`](../SKILL.md) invokes it. Existing references to a `divergent` verdict describe the old process; intentional local differences now remain context for the normal workflow. There is no separate classification or verdict record to maintain.
