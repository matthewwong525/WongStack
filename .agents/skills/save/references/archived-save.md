# Maintain an archived handoff

Load when the session supplied the archive path or evidence uniquely resolves it. Keep that exact `CHANGE_ROOT`; recover `NAME` from the dated folder only if the caller did not supply it. Branch and change names remain independent.

The archive is the completed plan. Set Status to `ready-to-ship`, keep the actual Branch, and append a dated entry for the archive checkpoint. Do not reconstruct its plan from the conversation, author a new plan, or recreate an active folder. Capture new surrounding session context only under the main note rule.

Refresh the review with the shared builder. Skip active delta reconciliation: the CLI archive already handled it. Stage the archive plus the corresponding tracked removals and implementation paths. The PR renderer uses archive mode, follows review links into the archive, and omits the continue command. Save reports the archived path and exact gate result; ship alone can merge.
