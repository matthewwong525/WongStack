---
name: "OPSX: Sync"
description: Sync delta specs from a change to main specs
category: Workflow
tags: [workflow, specs, experimental]
---

Invoke the `openspec-sync-specs` skill (via the Skill tool) and follow it verbatim — that skill owns this command's behavior. Anything after `/opsx:sync` is the change name; pass it through.
