---
name: "OPSX: Archive"
description: Archive a completed change in the experimental workflow
category: Workflow
tags: [workflow, archive, experimental]
---

Invoke the `openspec-archive-change` skill (via the Skill tool) and follow it verbatim — that skill owns this command's behavior. Anything after `/opsx:archive` is the change name; pass it through.
