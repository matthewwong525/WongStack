---
name: "OPSX: Propose"
description: Propose a new change - create it and generate all artifacts in one step
category: Workflow
tags: [workflow, artifacts, experimental]
---

Invoke the `openspec-propose` skill (via the Skill tool) and follow it verbatim — that skill owns this command's behavior. Anything after `/opsx:propose` is the change name (kebab-case) or a description of what to build; pass it through.
